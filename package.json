{
  "name": "setup-nix",
  "version": "1.0.0",
  "description": "Setup Nix on the GitHub Actions runner",
  "main": "main.js",
  "scripts": {
    "build": "npm run build:pre && npm run build:main && npm run build:post",
    "build:pre": "ncc build pre.js -o dist/pre && cp install-nix.sh dist/pre/",
    "build:main": "ncc build main.js -o dist/main",
    "build:post": "ncc build post.js -o dist/post"
  },
  "dependencies": {
    "@actions/cache": "^4.0.0",
    "@actions/core": "^1.11.0",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
